export const scaffoldProposalTemplate = (): string => `## Why
<!-- Explain the problem or opportunity this change addresses.
     What pain point are we solving? Why is this change necessary? -->

## What Changes
<!-- List the concrete changes this proposal introduces:
     - New features being added
     - Existing behavior being modified
     - Functionality being removed

     Be specific about what will change in the codebase. -->

## Impact
<!-- Identify the areas affected by this change:
     - Affected specs: \`specs/[capability-name]\`
     - Affected code: \`src/[module-path]\`
     - Breaking changes (if any)
     - Migration steps (if needed) -->
`;
